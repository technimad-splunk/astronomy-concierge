# local-agent-galileo

<!-- TODO: Replace this line with a one-sentence description of what this project does. -->

## Goal

<!-- TODO: Describe the project's purpose in a short paragraph.
     Answer: what problem does it solve, and why does it exist?
     Example:
       local-agent-galileo is a … that enables … so that … .
-->

## Installation

<!-- TODO: List exact steps to get from zero to a running project.
     Include: prerequisites (runtime versions, tools), environment variables, setup commands.
     Example:
       ### Prerequisites
       - Node.js >= 20
       - An OpenAI API key in OPENAI_API_KEY

       ### Steps
       ```sh
       git clone https://github.com/your-org/local-agent-galileo
       cd local-agent-galileo
       npm install
       cp .env.example .env   # fill in your keys
       ```
-->

## Example usage

<!-- TODO: Show at least one concrete, runnable example of the primary use case.
     Example:
       ```sh
       npm run dev
       ```
       or a code snippet demonstrating the main API / CLI command.
-->
