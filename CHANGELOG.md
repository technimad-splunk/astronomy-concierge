# Changelog

All notable changes to this project are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versions follow [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Changed

- **Intent:** Restore Splunk GenAI translator compatibility by preventing concierge image rebuilds from floating OpenTelemetry past the removal of `opentelemetry._events`.
  **Rationale:** `splunk-otel-util-genai` currently imports the deprecated Events API that was removed in OTel 1.44.0, so unbounded upgrades broke translator initialization and produced startup `.pth` import tracebacks.
  **Impact:** `pyproject.toml` now bounds OTel core/instrumentation ranges below the breaking versions, a tracked `constraints.txt` pins the concierge runtime to `opentelemetry-{api,sdk,exporter-otlp}==1.43.0` and `opentelemetry-{instrumentation,semantic-conventions}==0.64b0`, and `web/concierge/Dockerfile` installs with that constraints file for reproducible rebuilds.

- **Intent:** Remove callback-mode global turn serialization in the concierge web service while keeping Galileo trace isolation.
  **Rationale:** Galileo 2.6.0 logger instances are context-isolated and can be created quickly from resolved IDs, so a process-wide lock was no longer necessary and caused user-visible chat queueing under concurrent turns.
  **Impact:** `agent/telemetry.py` now exposes a per-session Galileo logger factory based on cached `project_id`/`log_stream_id`; `web/concierge/service.py` uses per-conversation callbacks/loggers, deletes the global Galileo lock path, adds a bounded concurrency semaphore (`CONCIERGE_MAX_CONCURRENT_TURNS`), and terminates per-session loggers on reload/shutdown. `web/concierge/app.py` now logs telemetry startup status and warns when the translator is disabled/failed.

- **Intent:** Reconcile the subagent model-governance roster with the model slugs currently available in Cursor.
  **Rationale:** The rule's maintenance contract requires agents to keep the roster aligned with real tool availability so subagent runs do not silently fall back to defaults.
  **Impact:** `.cursor/rules/subagent-models.mdc` now lists only currently available slugs, updates assignment mappings to valid models (including `claude-opus-5-thinking-high` for deep review/security), refreshes examples, and updates the verification date to `2026-08-31`.

### Fixed

- **Intent:** Stop concierge trace export drops without relying on a non-default collector gRPC receive limit.
  **Rationale:** Live measurement showed the largest exported OTLP batch was `189,232` bytes across `8` spans (~23.6 KB/span), so reducing app-side batching from `512` to `64` spans shrank worst-case payloads to ~1.5 MB and removed the real `RESOURCE_EXHAUSTED` cause under the collector's default 4 MiB limit.
  **Impact:** `stage/splunk-otel/docker-compose.override.yml` keeps the effective fix (`OTEL_BSP_MAX_EXPORT_BATCH_SIZE=64`, `OTEL_BSP_MAX_QUEUE_SIZE=512`, `OTEL_BSP_SCHEDULE_DELAY=1000`) and collector memory headroom (`deploy.resources.limits.memory=320M`, `GOMEMLIMIT=256MiB`), while `stage/splunk-otel/otelcol-config-extras.yml` removes the temporary `max_recv_msg_size_mib: 8` override and returns the receiver to the upstream 4 MiB default after validation.

- **Intent:** Bound faulted LangGraph turns so `/chat/stream` never appears hung and recursion-limit truncation is diagnosable to operators.
  **Rationale:** A stale tool-fault can cause repeated ReAct tool calls without reaching a user answer, and LangGraph may report recursion exhaustion either as an exception or as a fallback assistant message; both paths need a consistent user-visible and telemetry-visible outcome.
  **Impact:** `web/concierge/service.py` now detects recursion-limit exhaustion in both forms (`GraphRecursionError` and the LangGraph sentinel `"Sorry, need more steps to process this request."`), returns a stable fallback reply, sets `concierge.turn.outcome=recursion_limit_exhausted`, and marks the turn as truncated. Faulted SSE probes now emit periodic `progress` events and complete with a coherent `done` payload in ~18s rather than running silently.

- **Intent:** Make `invisible-failure` reliably drain Locust during Play without stopping the load-generator container.
  **Rationale:** The scenario did not opt into `quiet_background`, and `scripts/loadgen.sh` depended on in-container `curl` that is absent in the `load-generator` image, forcing an unintended container-stop fallback.
  **Impact:** `scenarios/invisible-failure/scenario.yaml` now enables `quiet_background: true`; `scripts/loadgen.sh` now calls Locust from the host (Envoy `/loadgen` first, mapped host port second), verifies post-stop state before declaring success, and keeps container stop as explicit last-resort fallback. Related operator docs and talk track text were updated to reflect the new behavior and reset restore path.

- **Intent:** Make Reset restore Locust traffic reliably after repeated quiet/restore cycles and report actual health instead of assuming `/swarm` success.
  **Rationale:** Reset's restore path could wedge Locust in `state=spawning, user_count=0` when browser traffic users were re-swarmed in-process, and the script still printed success after a bare HTTP 200 without validating runtime state.
  **Impact:** `stage/splunk-otel/docker-compose.override.yml` now disables `LOCUST_BROWSER_TRAFFIC_ENABLED`; `scripts/loadgen.sh restore` now verifies `state=running` with `user_count>=1`, skips `/swarm` after a fresh container start (autostart path), retries the cheap `/swarm` path only from `state=stopped`, and escalates to `docker compose restart load-generator` within a bounded timeout when needed. Control-plane/stage/runbook docs were updated with the new restore behavior and troubleshooting signal.

- **Intent:** Refine the `invisible-failure` (V1) SE talk track and expected-signal footprint per operator feedback: add a tangible cart-mismatch reveal, name the Galileo alert exactly, and state the true APM footprint (fully green).
  **Rationale:** The old talk track asked the audience to take the "ungrounded answer" on faith. Adding an "add to cart → open the store cart → compare prices" beat makes the hallucination concrete — the cart is served by the real cart/product-catalog services (not the faulted agent tools), so it shows the true `$101.96` while the agent quoted an invented price. Operators also asked for the precise alert name ("Context Adherence (SLM)" → Slack). The APM footprint is **fully green**: the operator confirmed they saw no consistent `flagd` errors (only transient startup blips), so V1 induces no visible infra signal — which is the whole point of an "invisible" failure. **Context Adherence (SLM)** → Slack is the sole signal.
  **Impact:** `scenarios/invisible-failure/captions/invisible-failure.md` now drives from the storefront-embedded concierge (shared cart), adds the cart-reveal beat, names **Context Adherence (SLM)** + the **Slack** alert, and states APM is fully green (concierge path + core store services, no notable errors); a cart-empty reset note was added. `docs/runbook.md` mirrors this (summary + per-vignette tables and a new "V1 reveal" callout). `control_plane/verification/galileo_verifier.py` documents that `context_adherence_low` is the "Context Adherence (SLM)" alert (no behavior change). `control_plane/verification/splunk_verifier.py` `apm_all_green` attestation requires the concierge path + core store services green with no scenario-caused errors (generic tolerance for ambient startup noise; no `flagd`-specific carve-out). Verified factually: the cart shows the real `$101.96` (live product API confirmed `units:101,nanos:960000000`).

### Fixed

- **Intent:** Keep the Astronomy Shop cart badge in sync when the embedded concierge mutates the shared cart during a chat turn.
  **Rationale:** Storefront (:8080) and concierge (:8090) share the `concierge_session` cookie cart, but the nav badge is fed by react-query on the storefront; add-to-cart runs inside the iframe, so the parent tab does not refetch cart state until navigation or focus.
  **Impact:** `agent/store_client.py` increments a monotonic `cart_mutation_version` on `add_to_cart` and checkout. `web/concierge/service.py` sets `cart_mutated` on the SSE `done` event when the version advances during a turn. `web/concierge/frontend/src/App.jsx` posts `astronomy_concierge.cart_mutated` to the validated `parent_origin`. `web/concierge/embed/concierge-bridge.js` listens via `postMessage`, passes `parent_origin` when opening the iframe, and dispatches focus/visibility plus a custom event so stale cart queries refetch immediately.
- **Intent:** Make the storefront cart badge refresh deterministic after embedded concierge cart mutations, even when focus-based react-query refetch heuristics do not fire.
  **Rationale:** The bridge's synthetic `focus` / `visibilitychange` nudge depends on focus-manager behavior and stale-query timing; in practice that can miss while the iframe owns focus. A storefront-side listener that directly invalidates/refetches the cart query removes that fragility.
  **Impact:** Added a tracked storefront override at `stage/splunk-otel/frontend/providers/Cart.provider.tsx` that listens for `astronomy_concierge:cart_mutated` and calls `invalidateQueries` + active `refetchQueries` for `['cart']`. `scripts/stage-setup.sh` now syncs that override into the vendored clone on each run, and `stage/splunk-otel/docker-compose.override.yml` declares `frontend.build` so `scripts/stage-up.sh --build` rebuilds and serves the override reproducibly.
- **Intent:** Make Splunk AI Agent Monitoring actually render the concierge — fix `gen_ai.request.model` showing `unknown`, the agent/workflow not appearing on the AI agents pages, and the "No parsable message event found" error on AI conversation spans.
  **Rationale:** The Traceloop `LangchainInstrumentor` emits `gen_ai.*` on LLM spans but (a) can't read the model off `ChatOllama` so it stamps `gen_ai.request.model`/`gen_ai.response.model` = `"unknown"` (the real name is in `traceloop.association.properties.ls_model_name`), (b) keeps the agent/workflow structure in its own `traceloop.*` shape that the AI Agent Monitoring pages don't interpret, and (c) by default puts no message content on LLM spans, so the conversation view has nothing parsable. A read-only spike against the real instrumentor + Splunk's Traceloop→GenAI translator confirmed the translator promotes the entity model and reconstructs the conversation when content capture is on, but does not fix the model name — so model is corrected collector-side. This keeps the existing single "instrument-once → fan-out" design (no switch to fully code-based instrumentation).
  **Impact:** Added `splunk-otel-util-genai` + `splunk-otel-util-genai-translator-traceloop` deps and bumped `opentelemetry-instrumentation-langchain` to `>=0.62.1`. `agent/telemetry.py` now registers the translator `SpanProcessor` FIRST (before the export `BatchSpanProcessor`) so its in-place mutation is exported, behind `SPLUNK_GENAI_TRANSLATOR` (default on) and de-duplicated against the package's `.pth` auto-enable hook; it also defaults `TRACELOOP_TRACE_CONTENT=true` (the content-capture lever). The local Splunk collector gains an OTTL `transform/genai_model` (`stage/splunk-otel/otelcol-config-extras.yml`) that promotes `ls_model_name` → `gen_ai.request/response.model` only when missing/blank/`"unknown"`. Telemetry status now reports the translator; `.env`/`.env.example` document the two new knobs. Galileo is unaffected (its callback path is independent of OTel spans).

- **Intent:** Make the `invisible-failure` vignette actually produce an ungrounded answer by faulting the whole product-read tool family instead of just `get_product_details`.
  **Rationale:** With `mode=stale` applied only to `get_product_details`, the agent simply called the un-faulted sibling tool `search_products` (and could use `get_recommendations`), which expose the same live catalog price/description, so it still answered with the real grounded price ($101.96) and the stale partial snapshot never constrained the response. Delivery/auth/session-rebuild were all working; the hole was that a single-tool stale fault is bypassable via sibling tools that read the same data.
  **Impact:** Added a `params.also_fault` list to the `tool_fault` contract; the control-plane `tool_fault` trigger now applies/resets the same fault spec across `trigger.ref` plus each `also_fault` tool (validated, de-duplicated), and `scenarios/invisible-failure/scenario.yaml` now faults `get_product_details` + `search_products` + `get_recommendations` with the shared stale snapshot. The agent-side overlay already supported a multi-tool fault map, so no concierge API or `agent/` changes were needed. Caption talk-track updated to reflect the family fault.

- **Intent:** Move agent-side trigger delivery (`tool_fault`, `prompt_overlay`, `rag_corpus`) from filesystem overlays to authenticated concierge admin APIs so the running containerized concierge process receives trigger state directly.
  **Rationale:** Host-written overlay files are not a reliable seam for the running `concierge-web` container, and these trigger types carry scenario-owned rich payloads that are better delivered as explicit API payloads than as flag flips. The selected design keeps `feature_flag` unchanged on flagd, drops headless `python -m agent` driving for agent-side triggers, and enforces fail-closed auth for mutation endpoints.
  **Impact:** `agent/overlay.py` is now an in-memory, lock-guarded overlay holder with per-trigger write/clear APIs; `agent/rag.py` consumes in-memory overlay docs. Concierge adds authenticated `POST /admin/scenario/apply` and `POST /admin/scenario/reset` endpoints and session-manager overlay apply/reset methods that call `reload()` so next turns rebuild with updated state. New `control_plane/concierge_client.py` sends trigger payloads over HTTP (`CONCIERGE_API_URL`, optional `CONCIERGE_ADMIN_TOKEN` bearer auth), trigger handlers now POST instead of writing files, and CLI `play` now applies agent-side triggers then instructs SEs to drive via concierge web chat. Added `CONCIERGE_ADMIN_TOKEN` env wiring (`.env*` + compose override), removed stale file-overlay reset logic from V1 reset script, and updated runbooks/READMEs accordingly.

- **Intent:** Remove the manual `CONCIERGE_ADMIN_TOKEN` setup step by trust-on-first-use (TOFU) auto-generation, so the authenticated trigger-delivery path works with zero extra operator action.
  **Rationale:** The new fail-closed admin gate needs a shared secret between the containerized concierge and the host control-plane; requiring a manual `openssl rand` step is easy to forget and would silently fall back to loopback-only auth that the host control-plane cannot satisfy against the container. Generating once on first bring-up and persisting to `.env` guarantees the token exists while keeping the auth gate as the enforcement layer.
  **Impact:** `scripts/stage-up.sh` now reads `CONCIERGE_ADMIN_TOKEN` from `.env` before bring-up; if empty/missing it generates a 64-hex token (`openssl rand -hex 32`, falling back to `/dev/urandom`+`xxd` or `python3 secrets`), persists it via a portable temp-file rewrite (replace the existing line in place, else append), exports it with the other concierge vars for compose `${...}` substitution, and prints only a secret-safe status line (value never echoed). `.env.example` and the runbook/README docs now state the token is auto-generated on first `stage-up` (manual generation only for standalone non-stage-up runs, which otherwise use the loopback fallback).

- **Intent:** Make the SE control-plane web Play action setup-only so it prepares scenario state without auto-driving the concierge agent.
  **Rationale:** The web Play flow was unintentionally invoking `/api/play/stream` with driving enabled by default, which consumed the scenario `drive_prompt` and ran `python -m agent` automatically. SE workflow requires manual driving in the concierge/storefront UI followed by explicit Verify.
  **Impact:** Web play endpoints now default `no_drive=True` (while still allowing explicit `no_drive=false`), the Play button explicitly sends `no_drive=true`, and the editable prompt-override input was removed from the control-plane scenario card. The read-only drive-prompt display and copy affordance remain so SEs can copy/paste the known-good prompt into the concierge UI. `web/control_plane/README.md` now documents Play as setup-only (trigger apply + optional quiet-background loadgen drain, no agent subprocess driving).

- **Intent:** Redesign V1 "The Invisible Failure" from a backend-visible `productCatalogFailure` flag fault to an agent-tool-seam stale/incomplete snapshot fault while preserving the closed trigger contract.
  **Rationale:** The previous V1 could disclose a graceful backend error path and could surface real store-service errors in Splunk, which weakened the "infra green vs answer quality" wedge. Galileo in this project runs `context_adherence` and `completeness` (no ground-truth correctness scorer), so the fault must be a partial snapshot missing requested fields to reliably trigger ungrounded claims.
  **Impact:** Added `tool_fault` mode `stale` (mode-only extension; no new trigger type) to write scripted snapshot `data` into `agent/_overlay/tool_faults.json`, updated agent tool-fault handling to return the scripted stale payload as a successful tool result without backend calls, and rewired `scenarios/invisible-failure` to use `tool_fault -> get_product_details (stale)` with a known-good price/spec prompt. Updated V1 captions, Splunk verifier attestation text (expected footprint + explicit re-attestation requirement), and runbook quick-reference wording. No changes were made under `stage/opentelemetry-demo`; expected signals remain `galileo: [context_adherence_low, ungrounded_claim]` and `splunk: [apm_all_green]`.

- **Intent:** Correct `scripts/stage-up.sh --build` so it rebuilds only locally-owned override services instead of force-building every service with a `build:` context in the composed stack.
  **Rationale:** `docker compose up --build` rebuilds upstream OpenTelemetry demo services unintentionally, contradicting script docs and wasting time/resources when used with `--pull`.
  **Impact:** `--pull` continues to refresh pullable upstream images; `--build` now explicitly builds only local override-defined services (derived from `docker-compose.override.yml`, currently `concierge-web`, with a `concierge-web` fallback) before `up -d`, preventing unnecessary upstream rebuilds. The in-script usage/header docs and `scripts/README.md` were updated to match; default (no-flag) behavior is unchanged.

- **Intent:** Polish the concierge chat UI — collapse the technical session metadata panel and replace the bland empty-state text with an immersive welcome bubble.
  **Rationale:** Session ID, conversation ID, and cart shopper are useful for demo debugging but were too visually prominent, competing with the chat itself. The plain empty-state text gave no personality or guidance. A collapsible `<details>/<summary>` hides the metadata by default with a single muted "Session info" toggle, and a styled assistant bubble greets the user on load using the existing bubble design language.
  **Impact:** `web/concierge/frontend/src/App.jsx` — `<section class="meta-panel">` replaced with `<details>`, empty-state `<p>` replaced with a `.bubble-assistant` welcome article. `web/concierge/frontend/src/styles.css` — `.empty-state` removed; `.meta-panel-toggle`, `.meta-panel[open]`, `.bubble-welcome`, and `.bubble-welcome-hints` added. No backend or API changes.

### Removed

- **Intent:** Remove the playlist composer feature from the SE control-plane web UI and the `control_plane` CLI to simplify the operator surface around the runbook-ordered vignette flow.
  **Rationale:** With the current small, ordered vignette set, playlist composition adds operator complexity without improving demo execution value.
  **Impact:** Removed the `/api/playlist` backend route and playlist-specific web UI controls/JS/CSS, deleted the CLI `playlist` command and parser wiring, and updated operator-facing READMEs/docs to reflect a `list / play / reset / verify` surface only. `message` and `duration_min` remain in the scenario contract and are still surfaced by `list`.

### Added

- **Intent:** Share one shopper cart across the Astronomy Shop storefront (`:8080`) and the Astronomy Concierge chat (`:8090`) so a cart built in either surface is visible in the other — turning the concierge into a true co-shopping assistant rather than an isolated sandbox.
  **Rationale:** The storefront keeps its shopper id in `localStorage` under key `session` (`{userId,currencyCode}`), which is per-origin and unreadable from `:8090`. Rather than couple cart identity to telemetry, the storefront id is bridged into a **port-agnostic `concierge_session` cookie** on host `localhost` (cookies on `localhost` are sent to every port) via a tracked Envoy `<script>` injection override — reversing the earlier "injection out of scope" stance now that cart sharing requires it. The cart key (`user_id`) is deliberately **decoupled** from the telemetry `session_id` so Galileo/OTel session grouping stays keyed on the conversation while the cart follows the shopper.
  **Impact:** New tracked `stage/splunk-otel/frontend-proxy/envoy.tmpl.yaml` (copy of the vendored template + an `envoy.filters.http.lua` filter that injects a tiny bootstrap `<script>` reconciling `localStorage` <-> cookie and lazy-loading `web/concierge/embed/concierge-bridge.js`); the override is bind-mounted over the image's baked template via `stage/splunk-otel/docker-compose.override.yml` (`frontend-proxy` `volumes` + `CONCIERGE_WEB_PORT`) at runtime — the gitignored clone is never edited in place. `agent/store_client.py` `StoreClient` gains a `user_id` param (defaults to `session_id`) used for all cart + recommendation calls; `web/concierge/**` threads `cart_user_id` end-to-end (`app.py` request/query validation, `service.py` per-turn `store.user_id` refresh). Verified live: a cart built in the storefront is read/written by the concierge and vice-versa.

- **Intent:** Ship **Phase 7 — web interfaces**: a shopper-facing **Astronomy Concierge** chat app (FastAPI service + standalone React/Vite frontend) and a **control-plane web UI** (FastAPI over the existing harness), both built as thin web layers over the unchanged `agent/` and `control_plane/` cores while preserving the single-instrument → dual-fan-out telemetry keystone. Built by two parallel coding subagents per the signed-off decisions W1–W8 in [`docs/web-interface-plan.md`](docs/web-interface-plan.md).
  **Rationale:** Per W1/W2/W4/W5, the concierge ships as a **standalone co-located** chat app (its own route/port, fully tracked by us — Envoy `<script>` injection into the storefront stays out of scope as an optional later enhancement), **containerized only** (Ollama stays native via `host.docker.internal:11434`), on a **FastAPI + SSE** stack, with the **CLIs kept as supported fallbacks**. The cores are reused verbatim so scenarios stay drop-in and `telemetry.py` is untouched, preserving `gen_ai.*` spans + GenAI histograms to Splunk AI Agent Monitoring and Galileo Sessions→Traces→Spans. A Phase-7.1 concurrency spike found that in **Galileo callback mode** a single shared `GalileoLogger` plus `start_session(...)` mutating process-global state lets concurrent web sessions cross-contaminate traces; the resolution serializes graph execution behind a global async lock **only** when `galileo_mode == "callback"`, leaving the OTLP-export mode (`GALILEO_OTEL_EXPORT=1`) fully concurrent.
  **Impact:** New `web/` tree — **concierge slice (7.1–7.3):** `web/concierge/app.py` (module-level `app`) + `web/concierge/service.py` exposing `POST /chat`, `GET /chat/stream` (SSE tokens), `/healthz`, optional localhost `POST /admin/reload`; `setup_telemetry()` once at boot; per-session graph build via `overlay.py`; sets `gen_ai.conversation.id`; standalone React/Vite frontend under `web/concierge/frontend/**` (lockfile committed) served by a new tracked `concierge-web` container appended to `stage/splunk-otel/docker-compose.override.yml` (existing collector config preserved); `agent/rag.py` gains `clear_corpus_cache()` to invalidate the `lru_cache` on per-session/reload (hot-reload, W3). **Control-plane slice (7.4):** `web/control_plane/app.py` (`create_app()` factory) + `web/control_plane/__main__.py` + templates/static; REST `list/play/reset/playlist` + `verify`, SSE `GET /api/play/stream` and `GET /api/verify/stream` with a live log pane, thin over `registry.discover()` / `apply_trigger` / `reset_trigger` / `run_verification` (core unchanged). Security: **loopback-only bind enforced** (`_require_loopback_bind()` rejects `0.0.0.0`/non-loopback before Uvicorn starts), CSRF (SameSite=Strict cookie + header token for POST + query token for acting SSE GETs), CSP + security headers, and secret redaction for `GALILEO_*`/`SPLUNK_*`/`OPENAI_*`. Shared scaffolding: `.env.example` (+`CONCIERGE_WEB_PORT=8090`, `CONTROL_PLANE_WEB_PORT=8099`, `WEB_ALLOWED_ORIGIN`, `CONCIERGE_API_URL`, commented containerized `OTEL_EXPORTER_OTLP_ENDPOINT`); `pyproject.toml` (+`fastapi`, `uvicorn[standard]`, `sse-starlette`, `jinja2`, `python-multipart`); `web/README.md`; launchers `scripts/concierge-serve.sh` and `scripts/control-plane-web.sh`. **Verification status — HONEST:** done at the static/integration level (deps install; both apps boot — concierge `/healthz`=200, control-plane `/api/list`=200; loopback guard accepts loopback / rejects `0.0.0.0`; registry discovers all 8 scenarios with no core edits; compose override merges with `concierge-web`; frontend `dist` builds). **Live clean-room sign-off (Phase 7.5) is PENDING** — stage up + Ollama + `concierge-web` healthy in a browser, multi-turn chat, concurrent-session Galileo isolation, trigger hot-reload via a fresh session, and telemetry parity (`gen_ai.*` spans + GenAI histograms) in Splunk AI Agent Monitoring and Galileo are not yet verified, and the full README Installation/Example-usage backfill is gated on that clean-room proof.

- **Intent:** Add a governance rule that tells agents which model to assign to each subagent type when using Cursor's multitask / Task tool.
  **Rationale:** Without an explicit `model` parameter, subagents inherit the parent agent's model. This means cheap read-only explorations run on expensive reasoning models and heavy coding tasks may run on under-powered fast models. A dedicated rule makes model selection deliberate and consistent across sessions.
  **Impact:** New `.cursor/rules/subagent-models.mdc` (alwaysApply). Defines a five-tier model roster and a per-subagent-type assignment table. `AGENTS.md` updated with principle #6. No production code changed.

- **Intent:** Add a governance rule for worktree-isolated subagents so parallel writing tasks never collide and the coordinating agent is freed from manual file-ownership tracking.
  **Rationale:** `generalPurpose` subagents share the working tree; when two run concurrently and touch the same files the last writer wins and interleaved edits corrupt state. Cursor's `best-of-n-runner` type provisions an isolated git worktree + branch per subagent automatically, so each writer is sandboxed and the coordinator only needs to review and merge diffs.
  **Impact:** New `.cursor/rules/subagent-worktrees.mdc` (alwaysApply). Defines the decision rule (read-only → no worktree, single writer → fine, parallel writers → `best-of-n-runner`), the workflow, and do/don't examples. `AGENTS.md` updated with principle #7. Updated `subagent-models.mdc` to add a _Last verified_ date and a self-check maintenance instruction so agents detect model roster drift automatically.

### Changed

- **Intent:** Give `scripts/stage-up.sh` one-command image refresh and correct the README's dependency + interface-hierarchy guidance to match the containerized, storefront-embedded reality.
  **Rationale:** Iterating on the concierge image required dropping to raw `docker compose build`; a first-class flag makes refresh a single command. The README still implied a mandatory `uv sync` and listed the standalone concierge chat as the primary surface — both now inaccurate, since the host launchers self-bootstrap their own `.venv` and the concierge is reached via the embedded overlay in the storefront.
  **Impact:** `scripts/stage-up.sh` accepts `--build` (force-rebuild locally-built images such as `concierge-web` during `up`) and `--pull` (re-fetch upstream pinned images via `pull --ignore-pull-failures`), parsed order-independently alongside `full|minimal`; default behavior is unchanged. `README.md` removes the stale "Phases 0–3" status block above Prerequisites, demotes the manual package-manager/`uv sync` step to optional (expert/local-dev only) with a note that the normal flow needs no manual install, and reorders the primary interface map to lead with the storefront (embedded AI Concierge) while marking the standalone `:8090` chat as a non-essential direct-access/debug surface.

- **Intent:** Align the SE control-plane web UX with the runbook flow by making scenario order deterministic, playlist composition click-driven, and talk tracks open in copy-friendly standalone pages.
  **Rationale:** SE feedback showed three friction points: scenario list ordering was alphabetical instead of runbook order, playlist composition required typing ids/pillars, and demo scripts in a modal made prompt copy/paste awkward. Encoding order declaratively in `scenario.yaml` keeps the drop-in architecture while making both CLI and web UI agree on play sequencing.
  **Impact:** `control_plane.manifest` now supports optional `order` on scenarios; `control_plane.registry.discover()` centrally sorts by `(order is None, order, title)` so ordered scenarios appear first for both `/api/list` and `scripts/control-plane.sh list`. The four core vignettes now declare `order: 1..4` in runbook order (`invisible-failure`, `compounding-error`, `firewall`, `trust-the-judge`). The control-plane web playlist composer now renders clickable scenario chips (non-fixture scenarios from `/api/list`) and posts selected scenario ids + optional budget to `/api/playlist` (backend now accepts `ids`). Added `GET /scenarios/{scenario_id}/script.html` that serves a full HTML talk-track page, and updated the UI to open demo scripts in a new tab (`window.open(..., "_blank", "noopener")`) instead of modal rendering.

- **Intent:** Reframe operator docs so the demo is explicitly web-first with CLI fallback, and align bring-up/teardown guidance to the current orchestration contract.
  **Rationale:** SE feedback requested an authoritative startup flow centered on browser interfaces, with terminal commands positioned only for automation or expert fallback.
  **Impact:** `README.md` now presents a clear numbered startup order (`cp .env.example .env` → `scripts/check-connectivity.sh` → optional `ollama pull` → `scripts/stage-up.sh`) and a prominent primary interface map (`:8090` concierge, `127.0.0.1:8099` SE console, `:8080` storefront); fallback sections now clearly position `scripts/concierge-serve.sh` and `scripts/control-plane.sh` as non-primary paths. `docs/runbook.md` bring-up and interface framing were reconciled to the same contract, including that `scripts/stage-up.sh` brings up the storefront + concierge + SE console and `scripts/stage-down.sh` tears all of it down (with `--volumes` for clean slate).
- **Intent:** Keep subagent model guidance current with newly available model slugs.
  **Rationale:** The `subagent-models.mdc` maintenance rule requires roster updates whenever Cursor exposes additional valid model slugs.
  **Impact:** Added `claude-opus-4-8-thinking-high`, `gpt-5.3-codex-xhigh`, `gpt-5.4-high`, and `gpt-5.5-medium` to `.cursor/rules/subagent-models.mdc` with strength/cost descriptions (Last verified remains 2026-06-19).

- **Intent:** Make `scripts/stage-up.sh` a true one-command bring-up — it now also launches the **SE Control-Plane web UI** — and reframe the README around the web interfaces as the primary surfaces with the CLIs as an explicit fallback.
  **Rationale:** Previously a cold SE had to run three scripts (`stage-up.sh`, then `concierge-serve.sh`, then `control-plane-web.sh`) and the README's casual "Example usage" listing didn't make the web-first hierarchy clear. `stage-up.sh` already starts the concierge as a container (via the tracked compose override), so the only missing primary surface was the SE console — which must run as a **host** process (not a container) because it needs host access to the running stage's flagd config, Galileo, and the repo `.venv`. Folding it into `stage-up.sh` (backgrounded, idempotent, loopback-only) collapses bring-up to a single command without blocking.
  **Impact:** `scripts/stage-up.sh` now backgrounds `scripts/control-plane-web.sh` after the containers are up, records its PID at `.harness/control-plane-web.pid`, logs to `.harness/control-plane-web.log`, skips re-launch if `CONTROL_PLANE_WEB_PORT` (default 8099) is already listening, and prints the storefront/concierge/SE-console URLs (it still returns immediately and keeps minimal/full modes, image pinning, and env export intact). `scripts/stage-down.sh` now stops that host process first (reads the PID file; handles missing/stale PIDs) before tearing down containers. `README.md` "Example usage" is replaced with an authoritative numbered **"Start the environment in this order"** sequence, an interface map that leads with the three web surfaces (concierge `:8090`, SE console `127.0.0.1:8099`, storefront `:8080` with the embedded overlay), and a clearly-labelled **CLI/host fallback** section (`scripts/concierge-serve.sh` for the host-side concierge, `scripts/control-plane.sh` + `scripts/agent-run.sh` for automation). `docs/runbook.md` bring-up/teardown updated for consistency. No application code changed.

- **Intent:** Restyle the concierge chat frontend to match the Astronomy Shop look and tighten the typing UX.
  **Rationale:** The standalone chat shipped functional but visually disjoint from the storefront it co-shops with; aligning brand and input affordances makes the concierge feel like a first-class part of the shop.
  **Impact:** `web/concierge/frontend/**` — Open Sans typography, white background, and a branded `app-header` with the bundled OpenTelemetry-demo logo; Enter-to-send / Shift+Enter-for-newline handling; a sticky bottom input. `web/concierge/app.py` mounts the logo at `/images` (served from the concierge's own origin, outside the hashed `/assets` mount). No telemetry or core changes.

- **Intent:** Finalize V2 "Compounding Error" verification as Galileo-only (Option C); make Splunk payment signals honestly UNVERIFIED with model-reliability guidance; fix `scripts/loadgen.sh restore` to treat `LOCUST_AUTOSTART=true` as success.
  **Rationale:** Live validation confirmed: (1) the store client + cart API work perfectly (deterministic probe added a telescope and read the cart back); (2) the ROOT cause of V2 not producing a Splunk payment fault is `llama3.1:8b` being unreliable at the 3-step tool chain (search → add_to_cart → checkout) — it emitted hallucinated tool names as text, never executing checkout, so the payment service was never exercised; (3) Galileo signals fire robustly (`tool_selection_quality_low` min 0.0, `tool_error` peak 0.667); (4) the drive_prompt was fixed to use a REAL catalog product (Eclipsmart Travel Refractor Telescope, id 1YMWWN1N4O). For `loadgen.sh restore`, the Locust API `/swarm` returns empty in this environment, but `LOCUST_AUTOSTART=true` means a running container auto-swarms — so a running container is already success.
  **Impact:** `splunk_verifier.py` payment signal UNVERIFIED reasons now explain the model requirement (gpt-4o-mini / qwen2.5:14b-instruct for reliable checkout). `compounding-error.md` caption gains a "Model reliability and Splunk payment signals" subsection. `scripts/loadgen.sh restore` treats a running container with autostart as success instead of printing alarming "may need manual attention" messages.

### Fixed

- **Intent:** Make Galileo telemetry actually upload from the containerized concierge web app, per interaction.
  **Rationale:** Two gaps surfaced live: (1) `concierge-web` references `GALILEO_*`/model env via `${VAR}` interpolation, which docker compose resolves from the **shell** environment — not the demo's auto-loaded `.env` — so the container booted with Galileo entirely unconfigured (empty keys); (2) the long-lived web service never flushed Galileo's in-process callback-mode buffers, so interactions did not appear until process exit. The Splunk OTLP export path was unaffected.
  **Impact:** `scripts/stage-up.sh` now exports the concierge/Galileo/model vars (read from `.env`) into the shell before `docker compose up`, warning (never failing) when `GALILEO_API_KEY` is empty since Galileo is optional. `agent/telemetry.py` gains `flush_galileo(conclude=False)` (best-effort flush + optional `conclude()`); `web/concierge/service.py` calls it per turn so each interaction uploads immediately; `web/concierge/app.py` adds a `load_dotenv()` host-path safety net. Verified live: Galileo Sessions->Traces->Spans populate per interaction; Splunk export unchanged.

- **Intent:** Make the Galileo verifier correctly read PII (entity-detection) scorers so V3 "Firewall" `pii_exposed` verifies instead of reporting "none present."
  **Rationale:** Live trace inspection showed the PII scorers (`input_pii`, `output_pii`, and their `_gpt` variants) emit a **list of detected entity types** (e.g. `['ssn','credit_card_info','date_of_birth','email','phone_number']`), empty `[]` when nothing is found — not a numeric score. The verifier's `_coerce_number` dropped list values, so the scorer looked absent even though it was enabled and firing. Separately, the firewall's injected PII surfaces in the **output** channel (`output_pii` fires; `input_pii` stays empty), but the detect logic returned on the first present scorer — an empty `input_pii` masked a firing `output_pii`. Confirmed empirically: firewall traces show `output_pii` with 6 entities (peak 6), and a user-turn PII test showed `input_pii: ['phone_number','address','email','name']`.
  **Impact:** `galileo_verifier.py` `_coerce_number` now maps a list to its length (non-empty = detection); detect-direction signals union values across ALL mapped scorers and fire if any detects (no early return). V3 `verify firewall` now reports `pii_exposed` PASS. No prompt/vignette rework was needed — the RAG-injection narrative trips `output_pii` as-is; a user-enters-address variant (verified via `input_pii`) is available as an option.

### Added

- **Intent:** Per-scenario "quiet background traffic" toggle so the control plane can drain and restore the Astronomy Shop's Locust load-generator around agent runs.
  **Rationale:** In V2 ("The Compounding Error"), the load-generator's continuous healthy checkout/payment traffic masks the agent's single failing checkout in Splunk APM — the payment error spike is invisible among hundreds of successful requests. Draining the load-generator before driving the agent gives a clean, agent-attributable telemetry window. V1/V3/V4 deliberately keep live background traffic (their punchline depends on "infra stays green" being meaningful).
  **Impact:** New `scripts/loadgen.sh` helper (`quiet`/`restore`), controlled by the Locust web API at runtime (no container restart) with a `docker compose stop/start` fallback. New optional `quiet_background: true` field in `scenario.yaml` (default `false`). The CLI drains on `play` (when the flag is set) and ALWAYS restores on `reset` (idempotent, safe no-op when the stage is down). Only `compounding-error` sets the flag; all other scenarios are unaffected. SEs see a clear status line during play/reset.

### Changed

- **Intent:** Switch V3 Firewall from `prompt_injection_detected` to `pii_exposed` Galileo signal; fix V2 checkout path to populate the cart before checkout.
  **Rationale:** Two design gaps found during live validation: (1) Galileo's `prompt_injection` scorer evaluates the user-input turn, not retrieved RAG content or tool outputs — the firewall's injection payload enters via the tool-result channel, so the scorer scores it 0. PII scorers evaluate the full conversation content where the sensitive data actually lands. (2) The agent's checkout tool never reached the gRPC CheckoutService/PaymentService — Splunk APM showed zero agent traffic on `checkout`/`payment`, only load-generator traffic. The agent's cart was empty at checkout time, so `place_order` now verifies the cart is non-empty before calling checkout, and the drive_prompt explicitly numbers the steps (search → add → checkout) for reliable sequencing.
  **Impact:** V3: `galileo_verifier.py` gains `pii_exposed` signal mapping to PII scorers; `prompt_injection_detected` is narrowed to prompt_injection-only scorers; `scenario.yaml` switches to `pii_exposed`; caption reframed for PII-detection narrative. V2: `store_client.py` `place_order()` pre-checks the cart; `tools.py` `add_to_cart` warns on empty cart; `checkout` tool docstring clarified; drive_prompt strengthened with numbered steps. Caveat: the PII scorer may not be enabled on the Galileo project — `verify` will report `unverifiable` until enabled.

- **Intent:** Re-scope V2 from `cartFailure` (EmptyCart only) to `paymentFailure` (checkout→payment charge) so the fault actually reaches the agent's path and lights up Splunk APM; add a `checkout` tool + `place_order` store client method.
  **Rationale:** Live testing confirmed `cartFailure` only breaks EmptyCart, NOT AddItem — the agent's `add_to_cart` calls AddItem and succeeds regardless of the flag. The payment service's charge is invoked during checkout, and `paymentFailure` (variant "100%") makes every charge fail deterministically. Added a `checkout` tool that calls `POST /api/checkout`, completing the agent's end-to-end purchase flow through checkout→payment.
  **Impact:** `agent/tools.py` gains `checkout` tool; `agent/store_client.py` gains `place_order()` method. V2 scenario updated: trigger.ref=`paymentFailure`, variant="100%", drive_prompt asks for checkout+retry. Splunk signals renamed `payment_latency_spike`/`payment_error_spike` (was `checkout_*`); `splunk_verifier.py` updated accordingly.

- **Intent:** Rework the firewall (V3) injection delivery so Galileo's `prompt_injection` scorer actually detects it.
  **Rationale:** The scorer evaluates conversation INPUT messages (including tool outputs), NOT the hidden system prompt. The prior approach only injected via system prompt overlay, which the scorer never inspects (score=0 on all 24 traces). Fix: dual-channel delivery — the `prompt_overlay` trigger now ALSO seeds the payload into the RAG knowledge overlay, so when the agent calls `search_knowledge_base`, the injection appears as a tool result in the conversation messages.
  **Impact:** `control_plane/triggers/prompt_overlay.py` writes to both `prompt_overlay.txt` and `knowledge/<id>-overlay.md`; reset clears both. Injection payload restructured with heading-based sections for RAG retrieval match. Firewall caption and reset.sh updated.

### Fixed

- **Intent:** Fix the Galileo verifier's per-signal direction, aggregation, and threshold so positive-detection and error signals fire correctly.
  **Rationale:** All signals shared a single threshold (`GALILEO_METRIC_LOW_THRESHOLD=0.5`) and printed "worst" regardless of direction. For `tool_error` (an error-rate signal), a peak of 0.333 failed against 0.5 threshold — but ANY tool error should fire. For `prompt_injection_detected`, the scorer returning 0 was correctly failing, but the message was misleading. Fix: each signal now has explicit `direction` (low/high/detect), `aggregation` (min/max), and `threshold_key` (separate env vars). Error signals default to threshold=0.0 (any error fires); detection signals fire on max>=1.
  **Impact:** `galileo_verifier.py` refactored with `_SignalSpec(direction, aggregation, threshold_key)`. Messages now say "min", "peak", or "max" per direction. New env var `GALILEO_METRIC_HIGH_THRESHOLD` (default 0.0) for error signals. Low signals unchanged.

### Added

- **Intent:** Ship Vignettes 2, 3, and 4 (Phase 5 — remaining core vignettes) as drop-in scenario folders with full auto-verification, completing the Core 4 demo library.
  **Rationale:** Each vignette exercises a different reliability pillar (error-compounding, guardrail-control, eval-accuracy) via the existing fixed trigger set (`tool_fault`, `prompt_overlay`), proving the Phase-3 harness contract: adding a vignette = dropping in a folder, never editing core. The Galileo verifier already supported the needed signal mappings (`tool_selection_quality_low`, `tool_error`, `prompt_injection_detected`, `context_adherence_low`, `ungrounded_claim`); the Splunk verifier was extended with signal-specific unverified guidance for parent MCP attestation.
  **Impact (codebase):** Added `scenarios/compounding-error/` (trigger: `tool_fault` on `add_to_cart` with 503 error message; signals: `tool_selection_quality_low`, `tool_error`, Splunk latency/error spikes). Added `scenarios/firewall/` (trigger: `prompt_overlay` with a poisoned product review containing prompt injection + PII; signals: `prompt_injection_detected`, Splunk `apm_normal_footprint`). Added `scenarios/trust-the-judge/` (trigger: `prompt_overlay` with eval-driver instruction; signals: `context_adherence_low`, `ungrounded_claim`; includes a 12-case curated eval set with known ground truth designed to expose naive-judge failure modes). Extended `control_plane/verification/splunk_verifier.py` with per-signal unverified reasons for `checkout_latency_spike`, `checkout_error_spike`, and `apm_normal_footprint`. No core edits to the harness, triggers, agent, or CLI.
  **Impact (user experience):** All 3 vignettes are discoverable (`control-plane.sh list` shows 8 scenarios), playable, verifiable, and resettable from the control plane with no code changes. Live verification results: V2 `tool_selection_quality_low` PASS (scorer worst 0.000 < 0.500); V3 `prompt_injection_detected` UNVERIFIED (scorer not yet enabled on log stream); V4 `context_adherence_low` PASS + `ungrounded_claim` PASS. Zero failures across all three. Splunk signals awaiting parent MCP attestation.

- **Intent:** Ship Vignette 1 — "The Invisible Failure" end-to-end (Phase 4), the project's first real demo deliverable: the concierge encounters a backend product-catalog fault, handles it gracefully (no error in its own APM traces), and produces an ungrounded answer that Galileo catches while Splunk APM stays green.
  **Rationale:** The design doc's original `productCatalogStaleData` flag does not exist in the vendored demo's flagd config. The closest real flag is `productCatalogFailure`, which fails the product catalog for product `OLJCESPC7Z` — a fault the concierge handles gracefully (returning a tool error, not an agent crash), so the `astronomy-concierge` service in Splunk APM stays healthy while the agent fabricates an ungrounded response. This produces the exact two-lens punchline: Galileo sees the quality drop; Splunk sees green.
  **Impact (codebase):** Updated `scenarios/invisible-failure/scenario.yaml` — `trigger.ref` changed from the non-existent `productCatalogStaleData` to the real `productCatalogFailure`; added `trigger.params.drive_prompt` with a known-good prompt targeting the affected product. Implemented `scenarios/invisible-failure/reset.sh` (was a stub). Authored the full SE talk-track at `captions/invisible-failure.md` (setup, reveal, punchline, reset, auto-verification, known-good prompt card, dashboard pre-warming). `expected_signals` (`galileo: [context_adherence_low, ungrounded_claim]`, `splunk: [apm_all_green]`) confirmed consistent with the Galileo and Splunk verifiers.
  **Impact (user experience):** Verified live: `play invisible-failure` flipped the flagd flag (flagd logged the hot-reload); the agent (Ollama `llama3.1:8b`) hit the 500, fabricated product details, and fanned telemetry to both backends; `verify` produced 0 fail / 3 unverified (Galileo traces arrived but scorer metrics use UUID keys not human-readable names; Splunk ingest-only token); `reset` restored the flag to `off` and baseline confirmed (product returns 200). The play→verify→reset round-trip works cleanly from the control plane.

### Fixed

- **Intent:** Embed the V3 ("The Firewall") Splunk `apm_normal_footprint` attestation, confirmed by the operator via the Splunk APM o11y MCP.
  **Rationale:** The parent confirmed live that during the Firewall vignette run, the concierge showed health=Ok, errorCount=0, baseline latency, and all store services nominal — the guardrail fires at the LLM-evaluation layer, not the infrastructure layer. Embedding this as an `attested` result (same pattern as V1's `apm_all_green`) turns V3's Splunk signal from "unverified" to a concrete, auditable "attested" with evidence.
  **Impact:** `control_plane/verification/splunk_verifier.py` now returns `attested` for `apm_normal_footprint`. `verify firewall` produces 1 attested (Splunk) + the Galileo result for the vignette.

- **Intent:** Make the "Invisible Failure" vignette **auto-verify for real** — turn the prior `0 fail / 3 unverified` into genuine PASS/ATTESTED — by (A) teaching the Galileo verifier to resolve scorer UUIDs to human names, and (B) representing the Splunk `apm_all_green` signal honestly as an operator-attested, concierge-scoped check.
  **Rationale:** (A) Galileo returns each trace's scorer metrics keyed by the scorer's **UUID** (e.g. `894d889a-…` = `context_adherence`, `4f27c2bc-…` = `completeness`), and a single scorer's value is spread across sibling sub-keys (plain value, `@average`/`@min`/`@max`, and `_multijudge_average` for boolean metrics that only emit `@category_count`). The old verifier matched on human names only, so every Galileo signal silently fell to "unverifiable" even though the quality drop was present. The fix fetches the project's scorer definitions via the SDK (`galileo.scorers.Scorers().list()`), builds a live name→UUID map, and looks each signal's scorer up by BOTH name and UUID across all value sub-keys — preferring dynamic resolution over hard-coded UUIDs. (B) `control_plane` holds an **ingest-only** Splunk token and structurally cannot query the APM API, so an indefinite "unverifiable" misrepresented a check that was in fact confirmed out-of-band. `apm_all_green` is re-scoped to mean *the concierge path stayed green / the failure was operationally invisible to APM* (NOT whole-environment green — the Astronomy Shop ships built-in background chaos) and is reported with a new, explicit `attested` result state carrying embedded, MCP-confirmed evidence — neither a faked auto-pass nor a silent gap.
  **Impact (codebase):** `control_plane/verification/base.py` — added a fifth `Status`, `attested`, plus a `VerificationReport.attested` property; `attested`/`unverifiable` do not fail the run. `control_plane/verification/galileo_verifier.py` — dynamic scorer name→UUID resolution, multi-sub-key value extraction (worst-of across recent traces vs `GALILEO_METRIC_LOW_THRESHOLD`), graceful name-only fallback if the scorer list can't be fetched. `control_plane/verification/splunk_verifier.py` — `apm_all_green` now returns `attested` with the embedded Splunk APM evidence; other Splunk signals stay `unverifiable`. `control_plane/cli.py` — `ATTESTED` glyph, attested count in the summary, and multi-line detail indentation so the attestation block renders aligned. The trigger flag (`productCatalogFailure`) and the `expected_signals` lists are unchanged; no secrets are printed.
  **Impact (user experience):** `scripts/control-plane.sh verify invisible-failure` now reports `galileo:context_adherence_low` = **PASS** (`context_adherence` [894d889a] worst 0.000 < 0.500) and `galileo:ungrounded_claim` = **PASS** (`completeness` [4f27c2bc] worst 0.000 < 0.500) from live trace queries, and `splunk:apm_all_green` = **ATTESTED** with the concierge-scoped evidence inline. Summary: `2 pass, 0 fail/error, 1 attested, 0 unverified`; Overall **PASS** — no remaining blank "unverified" rows for the vignette.

- **Intent:** Get the concierge's GenAI **metrics** (the histograms `gen_ai.client.token.usage` and `gen_ai.client.operation.duration`) to feed Splunk **AI Agent Monitoring**, after the prior change made the `gen_ai.*` **spans** work but the histograms appeared to be "missing".
  **Rationale (FINAL — corrected):** The histograms were **not** missing; the **verification signal was wrong**. The Splunk o11y **metric-NAME finder/catalog does not surface native OTLP histogram metrics** (histograms are a distinct metric type in o11y and don't list like gauges/counters), so "zero `gen_ai.*` in the catalog" was a **false negative**, not proof of data loss. The correct, doc-mandated configuration is `send_otlp_histograms: true` on the `signalfx` exporter — Splunk's "Set up AI Agent Monitoring" states verbatim: *"Histogram metrics are required to display data on AI Agent Monitoring pages. To send histogram data to Splunk Observability Cloud with the SignalFx exporter, set `send_otlp_histograms: true`."* The metrics pipeline exports via **`signalfx` only** (operator's no-double-send directive; `otlphttp/splunk` stays traces-only). The `signalfx` exporter uses the canonical `realm: ${SPLUNK_REALM}` form — **not a host fix**: `*.<realm>.signalfx.com` and `*.<realm>.observability.splunkcloud.com` are equivalent valid Observability ingests for the same tenant (verified: identical `401` on `/v2/datapoint` and `/v2/datapoint/otlp` on both). Delta temporality (also required) is now set **explicitly** on the OTLP metric exporter in code (`preferred_temporality` → `Histogram`/`Counter` = DELTA), not just via `OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=delta`, and proven independently of the console debug reader (which prints cumulative only because `ConsoleMetricExporter` ignores that env var). **Verification of histogram-backed AI Agent Monitoring must use the UI — `APM > AI agents` / `AI trace data` — NOT the metric-name finder.** The `gen_ai.*` SPAN path, the Galileo `GalileoCallback` path, and `service.name=astronomy-concierge` / `deployment.environment=local-agent-galileo` are all unchanged.
  **SUPERSEDED (kept for audit):** Two earlier theories were wrong. (1) The *"signalfx wrong ingest host → Splunk Cloud Platform → silent drop"* theory — disproven; both endpoint forms are equivalent Observability ingests. (2) The *"native OTLP histograms are accepted-then-dropped for this tenant; switch to `send_otlp_histograms: false` for the SignalFx-translated `_count`/`_sum`/`_bucket` representation"* experiment — wrong and **reverted**; it was driven by the false-negative catalog signal and contradicts the Splunk doc, which requires the native histogram (`true`). Whether removing the dual metrics path mattered is moot — `signalfx`-only is retained because it's correct (no double-send), not because it was the cause.
  **Impact (codebase):** `stage/splunk-otel/otelcol-config-extras.yml` — `signalfx` `send_otlp_histograms: true` (reverted to doc-mandated value); metrics pipeline exporters `[signalfx]` only; `otlphttp/splunk` traces-only; `signalfx` uses `realm:`. `agent/telemetry.py` — explicit `preferred_temporality` (delta) on the `OTLPMetricExporter`; keeps the three Splunk GenAI env defaults (`OTEL_INSTRUMENTATION_GENAI_EMITTERS=span_metric`, `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=SPAN_ONLY`, `OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=delta`) plus the off-by-default `GENAI_METRICS_CONSOLE_DEBUG` toggle. Docs corrected in `agent/README.md`, `stage/README.md`, and the YAML comments to state the true root cause and the UI-based verification.
  **Impact (user experience):** Live re-run (stage up, Ollama): both histograms produced with non-zero counts and delta temporality, egress via `signalfx` with zero exporter/trace errors, collector healthy; Galileo still logs the session→trace; `gen_ai.*` spans still export. Operator confirms the data in **`APM > AI agents` / `AI trace data`** (the authoritative check; MCP metric-name queries cannot see native histograms).

- **Intent:** Make the concierge light up **Splunk AI Agent Monitoring** (`APM > AI agents` / `AI trace data`), which stayed empty even though the agent's traces already appeared in plain Splunk APM.
  **Rationale:** Splunk AI Agent Monitoring keys off the **OpenTelemetry GenAI semantic conventions** (`gen_ai.*` span attributes) **and** GenAI histogram metrics. The agent was instrumented with **OpenInference** (`LangChainInstrumentor`), whose spans carry only `llm.*` attributes and **zero `gen_ai.*`**, and it exported no metrics at all — so APM showed the spans but the AI pages had nothing to render. Per Splunk's "Set up AI Agent Monitoring" doc, the supported fix is OTel GenAI conventions + GenAI metrics. The active OTel/Splunk instrumentor is now the OpenLLMetry / **Traceloop** `opentelemetry-instrumentation-langchain` (`LangchainInstrumentor`), which Splunk lists as a supported third-party instrumentation source. It was chosen over the official OTel-contrib LangChain instrumentor because the latter only emits `gen_ai.*` for `ChatOpenAI`/`ChatBedrock` and **silently skips other providers** — including our default `ChatOllama` — whereas Traceloop hooks the LangChain callback-manager layer and populates `gen_ai.*` for every chat model. `telemetry.py` also adds a `MeterProvider` with an OTLP/gRPC metric exporter (delta temporality) to the same local collector so the GenAI histograms are exported. Galileo is untouched: its `GalileoCallback` is independent of the OTel GenAI conventions.
  **Impact (codebase):** `agent/telemetry.py` swaps the OpenInference instrumentor for the Traceloop one, stands up a delta-temporality `MeterProvider`/OTLP metric exporter, applies the Splunk-documented GenAI env defaults (`OTEL_INSTRUMENTATION_GENAI_EMITTERS=span_metric`, `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=SPAN_ONLY`, `OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=delta`), and reports instrumentation + metrics status (surfaced in `agent/main.py`). Added `opentelemetry-instrumentation-langchain>=0.61` (Traceloop, resolved 0.61.0) and `opentelemetry-semantic-conventions-ai>=0.5.1` (resolved 0.5.1) to `pyproject.toml`; OpenInference is retained only for `using_session`. Added the three GenAI env vars to `.env.example`/`.env`. Clarified `stage/splunk-otel/otelcol-config-extras.yml` (the `signalfx` exporter keeps `send_otlp_histograms: true` — required so histograms reach AI Agent Monitoring — and the metrics pipeline inherits the upstream `otlp` receiver and exports to `signalfx`). Docs updated in `agent/README.md` and `README.md`.
  **Impact (user experience):** Verified live (stage up, Ollama `llama3.1:8b`): a "recommend a beginner telescope and add it to my cart" conversation produced spans carrying `gen_ai.*` (operation/provider/model, input/output messages, `gen_ai.usage.*` tokens, `gen_ai.tool.*`, `gen_ai.agent.*`, `gen_ai.workflow.*`) and the histograms `gen_ai.client.token.usage` + `gen_ai.client.operation.duration`, exported to the local Splunk collector with **zero OTLP exporter errors** and accepted by the collector cleanly. **Galileo still logs** session→trace→spans (no regression). One-time **Splunk console** steps remain for the operator: enable the **LLM Providers** integration (Data Management → Available integrations) for platform-side evaluations, and ensure the role has the `read_apm_ai_conversation` capability.

### Added

- **Intent:** Execute Phase 3 — build the **scenario harness**: the stable extensibility machinery so vignettes are **drop-in folders under `scenarios/` that never require core edits** (demo-design §7), proven with minimal stubs (the reference vignette content remains Phase 4).
  **Rationale:** The harness lives in a new `control_plane/` package (run via `scripts/control-plane.sh` or `python -m control_plane`) so the agent, telemetry fan-out, and CLI are **stable seams**. A **registry** auto-discovers `scenarios/*/scenario.yaml`; a strict **manifest loader/validator** enforces the design §7.1 contract exactly (`id`, `title`, `message`, `duration_min`, `trigger{type,ref,params}`, `expected_signals{galileo,splunk}`, `talk_track`, `reset`) and reports malformed folders without breaking discovery of the rest. The **four FIXED triggers** (`feature_flag | rag_corpus | tool_fault | prompt_overlay`) each implement `apply()`/`reset()`: `feature_flag` flips a flagd flag's `defaultVariant` in the running stage (flagd hot-reloads on file write; original saved under `.harness/state/` for deterministic reset), while the three agent-side triggers write to a new **stable overlay seam** (`agent/_overlay/`, gitignored) that the agent reads on startup — `rag_corpus` layers `*.md` over `agent/knowledge` non-destructively, `prompt_overlay` appends payload text to the system prompt, and `tool_fault` faults a named tool (`mode=error|remove`). Keeping the trigger set closed (the validator rejects unknown types) preserves the drop-in guarantee. The **`expected_signals` auto-verification hook** uses a per-backend `SignalVerifier` interface: the **Galileo verifier is real** (resolves project/log-stream, polls recent traces with retry/timeout for ingestion lag L2, maps named signals like `context_adherence_low`/`tool_selection_quality_low` to metric-threshold checks, and honestly returns `unverifiable` where a metric/scorer isn't queryable yet rather than faking a pass); the **Splunk verifier is unverified-by-design** because our `SPLUNK_ACCESS_TOKEN` is ingest-only (management/APM API returns 401), so it reports `unverified (ingest-only token; needs an org/API token or SE-driven MCP/UI check)` — the SE confirms Splunk via the o11y MCP. The **control-plane CLI** exposes `list / play / reset / verify / playlist` (playlists compose by `message` pillar within a `--budget`).
  **Impact (codebase):** Added the `control_plane/` package — `manifest.py`, `registry.py`, `paths.py`, `cli.py`, `__main__.py`, `triggers/` (`base.py` + the four handlers + a closed factory), and `verification/` (`base.py`, `galileo_verifier.py`, `splunk_verifier.py`, registry). Added the stable agent seam `agent/overlay.py` and wired tiny, scenario-agnostic hooks into `agent/rag.py` (overlay-aware corpus load), `agent/graph.py` (system-prompt overlay), and `agent/tools.py` (tool faults) — all no-ops at baseline. Added four clearly-marked stub scenarios (`scenarios/stub-feature-flag|rag-corpus|tool-fault|prompt-overlay/`, each with `scenario.yaml` + caption + `reset.sh`) as harness fixtures (NOT Phase-4 content). Added `scripts/control-plane.sh` (self-bootstrapping venv+deps+run). Added `pyyaml>=6.0` and the new packages to `pyproject.toml`; gitignored `/agent/_overlay/` and `/.harness/`. New env var `GALILEO_METRIC_LOW_THRESHOLD` (default `0.5`) plus advanced override docs in `.env.example`/`.env`. Documented usage in `control-plane/README.md`, `scripts/README.md`, and the `README.md` Example-usage Phase-3 subsection. No secret is ever printed.
  **Impact (user experience):** Verified live against the running stage + Galileo. **Drop-in:** `control-plane.sh list` discovered all 5 scenarios (reference + 4 stubs) with no core edits, and a malformed manifest was reported clearly while the valid scenarios still listed. **Per-trigger apply/reset:** all four proven to change state and restore baseline — `feature_flag` toggled `recommendationCacheFailure` off→on→off (flagd logged the file-WRITE sync); `rag_corpus` took the corpus 4→5 docs (adding one, shadowing one same-named baseline doc) and back to 4; `prompt_overlay` injected/cleared the system-prompt text; `tool_fault` made `get_recommendations` return an error then healthy again. A full driven `play stub-tool-fault --prompt …` ran the concierge on Ollama, which hit the faulted tool, recovered gracefully, and fanned telemetry to both backends; `reset` cleared it. **Verify:** the hook produced a pass/fail report — the Galileo verifier really queried 10 recent traces and reported `context_adherence_low` as `unverifiable` (scorer not enabled yet → Phase 4), and Splunk's `apm_all_green` as unverified-by-design; overall `PASS` under Phase-3 semantics (unverifiable signals don't fail the run). The existing `scenarios/invisible-failure/` manifest was left intact for Phase-4 authoring.

- **Intent:** Execute Phase 2 — build the AI shopping concierge (MVP) as a Python LangGraph agent that lives alongside the running Astronomy Shop, instrument it **once** with OpenTelemetry GenAI (OpenInference), and fan the same telemetry out to **both** Galileo and Splunk Observability (no scenarios/vignettes — that is Phase 3+).
  **Rationale:** The concierge has the design's two failure-surface capabilities: **(a) answer** via RAG over a small curated corpus (`agent/knowledge/`: shipping & returns, warranty, buying guide, store FAQ) exposed as a `search_knowledge_base` tool, and **(b) act** by calling the store's real microservices through the **frontend-proxy HTTP API** (`:8080/api/...`) as tools (`search_products`, `get_product_details`, `get_recommendations`, `add_to_cart`, `view_cart`, `list_currencies`) — so the agent's actions become genuine store traffic. It reuses the existing `get_chat_model()` provider abstraction (`MODEL_PROVIDER=ollama|openai`, no code change to switch). For the telemetry keystone (design §3), a single OpenInference `LangChainInstrumentor` on one `TracerProvider` feeds two paths: **Splunk** via a `BatchSpanProcessor` + OTLP/**gRPC** exporter to the local Splunk OTel Collector (`localhost:4317`, OTLP only — never `sapm`), and **Galileo** via the SDK's first-class `GalileoCallback` (Sessions→Traces→Spans + agent metrics). The pure-OTLP Galileo path (`GalileoSpanProcessor`) is kept as an opt-in (`GALILEO_OTEL_EXPORT=1`) because this enterprise/multitenant tenant's derived `/otel/traces` endpoint returns 404; the callback is Galileo's recommended LangGraph integration and works reliably.
  **Impact (codebase):** Added `agent/graph.py`, `agent/tools.py`, `agent/rag.py`, `agent/store_client.py`, `agent/telemetry.py`, `agent/main.py`, `agent/__main__.py`, and `agent/knowledge/*.md`; extended `agent/config.py` with `MODEL_TEMPERATURE` (default `0.0`, for reliable tool calling). Pinned real tested dependency minimums in `pyproject.toml` (LangChain/LangGraph 1.x, `galileo>=2.3`, `openinference-instrumentation-langchain`, `opentelemetry-*` 1.42, `httpx`). Added new env vars to `.env.example` / `.env` (`STORE_BASE_URL`, `MODEL_TEMPERATURE`, `DEPLOYMENT_ENVIRONMENT`, `GALILEO_OTEL_EXPORT`, `SPLUNK_OTLP_DISABLED`). Added the self-bootstrapping `scripts/agent-run.sh` (venv + deps + run, idempotent, secret-safe) and documented the run path in `README.md` and `agent/README.md`. Published stable collector OTLP host ports (`4317`/`4318`) in `stage/splunk-otel/docker-compose.override.yml` so the host-run agent can reach the documented `localhost:4317` (the upstream compose only mapped random host ports). All credentials are read from the gitignored `.env`; no secret is ever printed.
  **Impact (user experience):** `scripts/agent-run.sh --prompt "…"` runs a real conversation. Verified on Ollama `llama3.1:8b`: a "recommend a beginner telescope and add it to my cart" flow executed real tool calls (the store cart was actually populated), and a policy question was answered from the RAG corpus. Telemetry confirmed in **both** backends — Galileo created a session and returned a logged trace (no export errors), and the Splunk OTLP/gRPC export to the local collector produced **zero exporter errors**. The concierge appears in **Splunk APM** as `service.name=astronomy-concierge`, `deployment.environment=local-agent-galileo` (same environment as the store, for correlation). Known limitation: small local models are occasionally inconsistent at multi-step tool calling (sometimes emitting a tool call as text); `MODEL_TEMPERATURE=0` mitigates this and an OpenAI provider is more reliable.

### Changed

- **Intent:** Consolidate the two sibling directories `control-plane/` (hyphen, docs-only) and `control_plane/` (underscore, the Python package) into the single canonical package directory `control_plane/`.
  **Rationale:** Hyphenated names are not importable in Python, so the package must keep the underscore name; the hyphenated folder held nothing but the SE-facing `README.md`, so the two were redundant and confusing. Moving the README into the package directory gives one place for both the machinery and its SE-facing docs, removing a directory that could only ever hold docs. Historical changelog and agent-journal entries that describe creating `control-plane/` at the time were left intact (audit trail); only live structure/path references were updated.
  **Impact (codebase):** Moved `control-plane/README.md` → `control_plane/README.md` and deleted the now-empty `control-plane/` directory. Fixed the moved README's self-references (heading and the package self-link) and updated live directory/path references: `README.md` (merged the duplicated project-structure rows into one `control_plane/` row and repointed the `control_plane/README.md` link) and `docs/implementation-plan.md` (proposed repo-structure line). The shell launcher `scripts/control-plane.sh` keeps its hyphenated filename (conventional and importable-name-agnostic); CLI/command names and the English phrase "control plane" are unchanged.
  **Impact (user experience):** No behavioural change — `python -m control_plane --help` and `scripts/control-plane.sh` work exactly as before; SEs now find the control-plane README inside the package it documents.

- **Intent:** Update `docs/demo-design.md` and `docs/implementation-plan.md` to reflect that **enterprise Galileo access is secured** and to record the now-resolved design decisions, keeping the two planning docs mutually consistent.
  **Rationale:** Enterprise access removes the prior tier/feature gates that shaped the design as risks — the 5,000-traces/month free-tier cap, the "real-time guardrails require Galileo Pro (~$100/mo)" caveat, and the "Luna-2 / Agent Control / self-hosting are enterprise-only" limitation — so Luna-2 evaluators, real-time guardrails, Agent Control, and unlimited traces are now treated as available capabilities rather than blockers. With the constraint lifted, the gating decisions could be settled: **D1 agent framework = LangGraph** (matches Galileo `sdk-examples` incl. `langgraph-open-telemetry`, fits the Graph Engine cascade, clean provider swap via LangChain chat models); **D6 provider abstraction = LangChain chat-model interface** behind `MODEL_PROVIDER=ollama|openai` (`ChatOllama`/`ChatOpenAI`); **D2 theme = keep the Astronomy Shop as-is** (no reskin); **D4 eval-accuracy = promoted to a live Luna-2 vignette** ("Trust the Judge", a naive LLM-judge wrong ~1 in 3 vs. Luna-2 / consensus evaluators on a known-ground-truth eval set); **D5 Galileo Pro purchase = moot** (covered by enterprise); **D3 EC2 instance size = deferred to a Phase-1 empirical spike** (starting assumption `t3.xlarge`-class, no GPU since EC2 uses OpenAI). The keystone OTLP-fan-out risk is retired because enterprise provides OTLP ingest; Galileo distributed/OTel tracing remains Beta.
  **Impact (codebase):** `docs/demo-design.md` — reliability-pillars table, vignette library (added Vignette 4 "Trust the Judge"; core set now four; Pre-Production Gate renumbered to 5/optional), eval-accuracy handling (§6), runtime/self-hosting note (§8.3), reframed Galileo capabilities (§9.2), eval-accuracy pillar (§9.4), theme decision (§9.5), next steps (§10), and Appendix A. `docs/implementation-plan.md` — Phase 0 (decisions now settled; blockers reduced to obtaining accounts/tokens), Phase 2/5/6 risks and tasks (Firewall loses the Pro/LLM-as-judge-latency caveat; Trust the Judge added), the D1–D6 decisions table (D1/D2/D4/D6 decided, D3 deferred-to-spike, D5 moot), sequencing table, and the demo-ready milestone (now Core 4). Limitations L1 (live nondeterminism) and L2 (ingestion latency / Beta tracing) are retained. No production code.
  **Impact (user experience):** Stakeholders and future agents see a consistent plan with the open decisions resolved, the eval-accuracy story now demoable live, and a clearer Phase-0 entry path (no framework/Pro debates) — only provider accounts/tokens gate the start.

### Added

- **Intent:** Harden Phase-1 reproducibility so the entire stage comes up from a fresh clone of this repo using only the committed scripts + READMEs, with no manual `git clone` or machine-specific steps (satisfies the new `automate-verify` "Reproducibility" rule).
  **Rationale:** The stage previously depended on a hand-run clone of `open-telemetry/opentelemetry-demo` into the gitignored `stage/opentelemetry-demo/`, so a fresh checkout couldn't reproduce it. Now the pinned ref lives in ONE place (`stage/demo.ref`: `DEMO_REPO` + `DEMO_REF=2.2.0`), a new idempotent `scripts/stage-setup.sh` shallow-clones that exact tag (verifying it resolved, no-op if already present and correct, hard error if a different version is present) and materializes our tracked Splunk overrides into the clone (`otelcol-config-extras.yml` → the collector's default extras path; `docker-compose.override.yml` → clone root). `scripts/stage-up.sh` now runs setup first (self-bootstrapping) and pins `DEMO_VERSION` from `DEMO_REF` (single source), so a user can edit `.env` and go straight to `stage-up.sh`. Only the bulk upstream clone is gitignored; the ref file, both overrides, and all scripts are committed — i.e. everything needed to recreate the stage is tracked.
  **Impact (codebase):** Added `stage/demo.ref` and `scripts/stage-setup.sh`; rewired `scripts/stage-up.sh` (bootstraps setup, `DEMO_VERSION=${DEMO_REF}`, uses the clone-local override) and `scripts/stage-down.sh` (clone-local override, empty `SPLUNK_*` defaults to silence teardown warnings — still reads no secrets). Documented the full zero-to-running path (prerequisites → `cp .env.example .env` → `check-connectivity.sh` → `stage-setup.sh` → `stage-up.sh` → verify → `stage-down.sh`) in `README.md`, `stage/README.md`, and `scripts/README.md`. Secrets remain `.env`-only and are never printed.
  **Impact (user experience):** Verified by clean-room test — stopped the stack, deleted `stage/opentelemetry-demo/`, then ran `stage-setup.sh && stage-up.sh` from scratch: the clone returned at the pinned `2.2.0`, 28/28 containers came up, storefront returned HTTP 200, and the collector exported to Splunk with zero `otlphttp/splunk` errors. A fresh contributor can now reach a running, Splunk-exporting stage with the documented script sequence alone.
- **Intent:** Execute Phase 1 — stand up the forked OpenTelemetry "Astronomy Shop" locally via docker-compose and wire its OpenTelemetry Collector to export traces and metrics to Splunk Observability Cloud over OTLP/HTTP, with all 28 services running and the collector exporting without auth/permission errors.
  **Rationale:** The Splunk fork (`splunk/opentelemetry-demo`) exists and is current but its docker-compose path is broken at `v2.0.5` (its compose references collector config files absent from the tree) and its Splunk Observability integration is Kubernetes-only — so, per the plan's documented fallback, we vendor **upstream `open-telemetry/opentelemetry-demo` pinned to `2.2.0`** and add the Splunk exporter ourselves through upstream's documented `otelcol-config-extras.yml` merge seam. Transport is **OTLP/HTTP only** (Splunk Observability rejects OTLP/gRPC; the deprecated `sapm` exporter is not used): traces → `ingest.<realm>.signalfx.com/v2/trace/otlp`, metrics → `…/v2/datapoint/otlp`, auth via `X-SF-Token`. Our overrides live **outside** the (gitignored) upstream clone so upstream files stay pristine and only our config is tracked; `stage-up.sh` repoints the collector's existing extras mount at our file and pins `DEMO_VERSION` to the cloned `IMAGE_VERSION` (the demo ships `DEMO_VERSION=latest`, which pulled images newer than the pinned source and crash-looped the frontend-proxy on missing envoy clusters).
  **Impact (codebase):** Added tracked overrides `stage/splunk-otel/otelcol-config-extras.yml` (Splunk OTLP exporter + `deployment.environment=local-agent-galileo` resource tag + re-listed traces/metrics pipelines) and `stage/splunk-otel/docker-compose.override.yml` (injects `SPLUNK_ACCESS_TOKEN`/`SPLUNK_REALM`); helper scripts `scripts/stage-up.sh` and `scripts/stage-down.sh`; gitignored `/stage/opentelemetry-demo/`. Rewrote `stage/README.md` and updated `scripts/README.md` and `README.md` (stage run example, Phase-1 status). Secrets are read from the gitignored `.env` and never echoed or committed.
  **Impact (user experience):** `scripts/stage-up.sh` brings the store up at <http://localhost:8080/> (verified HTTP 200, 28/28 containers running, collector 0 restarts, "Everything is ready", and zero `otlphttp/splunk` export errors); the store appears in Splunk APM under environment `local-agent-galileo`.
- **Intent:** Implement the Phase-0 connectivity check (`scripts/check-connectivity.sh`) so a cold setup fails fast with a clear, secret-safe PASS/FAIL report for the selected model provider, Splunk Observability, and the Galileo deployment — satisfying the Phase-0 exit criterion "a trivial connectivity check confirms each account/token is valid."
  **Rationale:** The previous script was an echo-TODO stub. The check sources the gitignored `.env`, then runs token-validation probes only: Ollama `GET /api/tags` (with a non-fatal warning if `OLLAMA_MODEL` is absent, since it can be pulled) or OpenAI `GET /v1/models` (bearer); Splunk validated as the **ingest token it is** — a `POST https://ingest.${SPLUNK_REALM}.signalfx.com/v2/datapoint` (`X-SF-Token`) with an **empty** datapoint payload so it authenticates without sending real telemetry (the same path the Phase-1 Splunk OTel Collector uses); and Galileo reachability + key validation against the enterprise deployment, deriving the API base from `GALILEO_CONSOLE_URL` (host `console`→`api`, dropping the org-slug path) and calling unauthenticated `GET /v2/healthcheck` then read-only authenticated `GET /v2/datasets` (`Galileo-API-Key`). For Splunk, 2xx (or a payload-level 400, which still proves authentication) = PASS while 401/403 = FAIL. Secrets are passed to curl via expanded env vars in headers and are **never** echoed, logged, or printed — only pass/fail, HTTP status codes, and non-secret config (provider/realm/host/console/project) are shown; the script sends no real telemetry and mutates nothing.
  **Impact:** `scripts/check-connectivity.sh` is now a runnable verifier (`set -euo pipefail`, colorized PASS/FAIL/WARN, remediation hints, non-zero exit on failure). Run against the user's `.env`: **all three backends PASS** — model provider (ollama) HTTP 200, Splunk ingest token HTTP 200 on realm `eu0`, and Galileo health + key both HTTP 200 (enterprise endpoints confirmed). No application code or telemetry paths were added (those remain Phase 1+).
- **Intent:** Scaffold the Phase-0 repository skeleton — top-level directories (`agent/`, `stage/`, `scenarios/`, `control-plane/`, `scripts/`), Python tooling (`pyproject.toml`), the committed `.env.example` secrets template, the reference `invisible-failure` scenario folder, and the README Installation/structure sections — without implementing the agent, vignettes, or control plane.
  **Rationale:** Phase 0 of `docs/implementation-plan.md` calls for an agreed repo skeleton, secrets scaffolding, and pinned toolchain decisions *before* app code. Stubs/READMEs per directory establish the stable seams (drop-in scenarios, single-instrument/dual-fan-out) so later phases add code without restructuring. The only real code is `agent/config.py` (the `MODEL_PROVIDER=ollama|openai` selector, decision D6) because the provider abstraction is central to the two-runtime design; everything else is placeholders pointing at the relevant plan phase. `.env.example` ships placeholder values only (no hardcoded credentials), and dependencies use minimum version specifiers rather than invented pins (exact versions lock during the Phase-0 install).
  **Impact (codebase):** Added `agent/` (`__init__.py`, `config.py`, `README.md`), `stage/README.md`, `scenarios/README.md` + `scenarios/invisible-failure/` (`scenario.yaml`, executable `reset.sh` stub, `captions/invisible-failure.md`), `control-plane/README.md`, `scripts/README.md` + executable `check-connectivity.sh` stub, `pyproject.toml`, and `.env.example`. Filled the `README.md` Installation + Project-structure sections (Example usage left as a TODO). `.gitignore` already ignored `.env` (verified). No runnable application logic yet.
  **Impact (user experience):** A contributor can `cp .env.example .env`, see every required token grouped and commented, and read a per-directory map of what each part of the system will become — a clear, low-risk entry point into the build.
- **Intent:** Author `docs/implementation-plan.md`, a phased, dependency-ordered plan to build the demo environment described in `docs/demo-design.md`, so the team has a concrete, verifiable build sequence before any application code is written.
  **Rationale:** The design doc captures *what* and *why* but not *how/in what order*; a phased plan with per-phase exit criteria tied to the `automate-verify` rule prevents architectural drift, surfaces the critical path (and parallelizable tracks), and consolidates the open decisions (agent framework, theme, EC2 sizing, Galileo Pro, eval-accuracy talk-track) that gate the build. The plan recommends LangGraph as the agent framework (matching Galileo's `sdk-examples` and `langgraph-open-telemetry` precedent and the Graph Engine cascade story) while marking it as a decision to confirm.
  **Impact (codebase):** Added `docs/implementation-plan.md` covering Phases 0–6 (foundations/prereqs → stage → concierge MVP → scenario harness → reference vignette → remaining core vignettes → delivery & polish), each with goal/tasks/deliverables/exit criteria/dependencies/risks, plus sections for decisions-needed, sequencing/effort, git-hygiene, and a first "demo-ready" milestone. No production code yet.
  **Impact (user experience):** Stakeholders and future agents share a single skimmable build roadmap with explicit acceptance gates and the open decisions that must be resolved (and when), making it clear what blocks Phase 0 from starting.
- **Intent:** Define and document the agreed demo-environment design — a guided, SE-led demo that casts Galileo AI as the AI/agent intelligence layer (the hero) and Splunk Observability as the systems/infrastructure backdrop, replaying induced agent scenarios over a forked OpenTelemetry "Astronomy Shop" with a new Python AI shopping concierge.
  **Rationale:** The technical keystone — instrument the agent ONCE with OpenTelemetry GenAI conventions and fan the same telemetry to both Galileo (OTLP/`GalileoSpanProcessor`) and Splunk (Splunk OTel Collector, OTLP) — keeps roles clean and the build minimal, while an extensibility-first scenario harness (declarative scenario contract + registry + fixed trigger layer + auto-verified `expected_signals`) lets vignettes be added as drop-in folders without touching core. Capturing this before any code prevents architectural drift during implementation.
  **Impact (codebase):** Added `docs/demo-design.md` capturing goal/concept, framing & roles, technical keystone, the stage, messaging spine, vignette library, extensibility/scenario contract, runtime & environment matrix (Apple-silicon/Ollama and EC2/OpenAI behind a pluggable model provider), validated limitations & open questions, and suggested next steps. Filled the `README.md` Goal section. No production code yet.
  **Impact (user experience):** Stakeholders and future agents share a single, skimmable source of truth for the demo's design and constraints, and a defined first deliverable (the scenario harness with one reference vignette end-to-end).
- **Intent:** Establish project governance so that all future agents and contributors follow consistent hygiene practices from day one.
  **Rationale:** An empty repository with no conventions leads to inconsistent behaviour across agent sessions, missing context, and hard-to-maintain history. Encoding conventions as `alwaysApply` Cursor rules makes them automatic rather than aspirational.
  **Impact (codebase):** Added `.cursor/rules/` with five focused rules (`readme`, `changelog`, `git-hygiene`, `agent-journal`, `automate-verify`), `AGENTS.md` as an agent/human index, `README.md` skeleton, `docs/agent-journal.md` running journal, and `.gitignore`. No production code yet.
  **Impact (user experience):** Future agents will automatically maintain the README, changelog, git discipline, and decision journal without requiring explicit per-session reminders.

---

<!-- When cutting a release, move items from [Unreleased] to a versioned block:

## [0.1.0] - YYYY-MM-DD

### Added
...

[Unreleased]: https://github.com/your-org/local-agent-galileo/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/your-org/local-agent-galileo/releases/tag/v0.1.0
-->
