# Harness Stub — Stale Notice (NEW overlay doc)

This document is added by the `rag_corpus` harness stub to prove that a scenario
can inject a NEW corpus document over the baseline knowledge base. It is not real
store policy — it exists only to demonstrate the overlay mechanism.

## Stub marker
HARNESS_STUB_RAG_CORPUS_OVERLAY_ACTIVE
