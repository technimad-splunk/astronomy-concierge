# [harness stub] feature_flag trigger

**Not a vignette.** This is a Phase-3 harness fixture proving the `feature_flag`
trigger machinery: `play` flips the `recommendationCacheFailure` flagd flag in the
running stage (flagd hot-reloads), `reset` restores it. Real vignette talk-tracks
are authored in Phase 4+.
