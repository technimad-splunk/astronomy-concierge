#!/usr/bin/env bash
# Reset "The Invisible Failure" vignette back to baseline.
#
# Phase-0 stub. Phase 4 will implement this: clear the `productCatalogStaleData`
# feature flag so the product-catalog service serves fresh data again, and
# confirm the agent's next answer is grounded.
#
# See docs/implementation-plan.md Phase 4.
set -euo pipefail

echo "TODO(phase-4): reset invisible-failure — clear feature flag 'productCatalogStaleData' and restore baseline."
